name: Build, Test and Deploy in QA

on:
  push:
    branches:
      - develop

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: npm i